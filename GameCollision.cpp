#include "GameCollision.h"


GameCollision::GameCollision(void)
{
}


GameCollision::~GameCollision(void)
{
}

/*bool GameCollision::Collision()
{
	
	// This doesn't take anything but the distance at time of calculation. For moving objects more complicated collision detection should be required.

	//ObjVectorDif = MainLocation - *list->ComparedObjPos 

	//DistanceDif = ObjVectorDif.x * ObjVectorDif.x + ObjVectorDif.y * ObjVectorDif.y + ObjVectorDif.z; //Get the squared distance between the two objects

//	CollisionDist = MainRadius + *list->ComparedRadius; //Gets the minimum distance for a collision (must be squared later, to avoid having to perform a sqrt function)

//	return(dist <= CollisionDist * CollisionDist); //if true collision

}*/
